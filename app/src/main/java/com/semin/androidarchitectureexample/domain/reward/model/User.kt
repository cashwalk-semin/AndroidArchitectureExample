package com.semin.androidarchitectureexample.domain.reward.model

/**
 * Naming Rules
 * - 일반적으로 ${domain}의 네이밍을 갖는다.
 */
data class User(
    val name: String,
    val age: Int,
    val gender: Int,
)
